import React from "react";
import styled from "styled-components/native";

const Empty = styled.View`
  justify-content: center;
  align-items: center;
  min-height: 100%;
  text-align: center;
  background-color: #fff;
`;

const EmptyText = styled.Text`
  font-size: 20px;
`;

export { Empty, EmptyText };
